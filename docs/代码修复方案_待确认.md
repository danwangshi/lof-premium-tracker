# LOF 基金实时估值 —— 代码修复方案（待主 agent 确认）

> 生成时间：2026-08-07 ｜ 本文档列出拟对 `backend/estimator.py` 与 `backend/lof_index_map.json` 做出的修复/改动，供主 agent 确认后实施。
> 背景：此前已完成「官方跟踪指数调研」并更新 `docs/实时估值计算报告.md`。本次为把调研结论落到代码。

---

## 一、现状核实（结论修正）

对 `lof_index_map.json`（代码实际加载的静态映射）与官方调研结果做了**逐条联网实测**（push2delay.eastmoney.com `stock/get` 接口），修正了此前几处误判：

### 1. `2.xxx` 前缀（中证市场）在 push2delay 中有效，此前「30 个无效 secid」判定有误

`lof_index_map.json` 现有 **171 只**基金映射，其中 **30 只**使用 `2.` 前缀 secid（如 `2.930719`、`2.H30535`）。本次逐一实测：

- 30 个 `2.xxx` secid **全部返回实时行情**（f170 涨跌幅），并非无效格式。
- 东财 secid 前缀规则：`0.`=深证/国证、`1.`=沪深/中证、`2.`=中证主题/定制、`100.`=海外、`124.`=港交所。

**结论：静态映射 171 条 secid 全部有效，无需因「2.xxx」报修。**

### 2. 报告 index/fx+idx 表中「无行情」标注与代码实际行为不一致

报告「三、index」+「四、fx+idx」共 **188 只**跟踪类基金，标注分布：

| 标注 | 数量 | 代码实际行为 |
|---|---:|---|
| 正常（无标注） | 140 | 按对应指数估算 |
| 「无行情」 | **32** | 其中 **18** 只代码已有精确 `2.xxx` secid（实测可用，应改标）；**5** 只代码有代理指数（3 只 `100.HSI`、2 只 `100.SPX`）；**2** 只（160925/501050）可经 `2.CES300`/`2.950090` 报价但**未入映射**；**7** 只真实无行情（S&P/道琼斯海外行业指数等） |
| 「⚠️待核实」 | **16** | 11 只已在主动型黑名单；4 只因关键字误匹配需要入黑名单；1 只（501312 海外科技）保留 fx+idx 近似待人工确认 |

报告行 28 写的「官方指数可行情 137｜无行情 35｜待核实 16」与表格实际（140/32/16）略有出入，需一并修正。

### 3. 真实无行情仅 7 只（在跟踪表内）

**7 只**跟踪基金真实无实时行情源，退化 holdings/formal_lag 是合理兜底，不改：

- 161127 标普生物科技、162411 华宝油气、162415 美国消费、162719 石油LOF、501021 香港中小、501025 香港银行、501029 红利基金（均为 S&P/道琼斯海外行业指数或港股特殊指数，东财无报价）。

另有 160140 道琼斯REIT、161119 中债、167302 恒生大湾区、501310 标普沪港深价值 等**不在跟踪表内**（走 holdings/formal_lag），不涉及本次修复。

---

## 二、拟修复项

### 修复 1：`lof_index_map.json` 补充 2 只可报价基金

实测可用但尚未写入静态映射：

| 代码 | 名称 | 官方指数 | secid（实测 OK） |
|---|---|---|---|
| 160925 | 沪深港300LOF | 中华交易服务沪深港300人民币指数（CES300） | `2.CES300` |
| 501050 | 50AHLOF | 上证50AH优选人民币指数（950090） | `2.950090` |

- 写入后，这两只由「无行情→formal_lag」升级为「index 实时估算」。
- 仅新增 2 条映射，不动其他 169 条；`match_index` 静态映射优先，写入后关键字兜底不会覆盖。

### 修复 2：`_ACTIVE_FUND_BLACKLIST` 补充 4 只待核实主动基金

「待核实」16 只中，11 只已在黑名单，剩余 5 只中 **4 只**会被 `INDEX_KEYWORDS` 裸词误匹配成指数估算，应入黑名单：

| 代码 | 名称 | 当前关键字匹配 | 问题 |
|---|---|---|---|
| 160644 | 港美互联网LOF | "互联网"→0.399970（中证移动互联网） | 实际为港美主题 QDII，非中证移动互联网 |
| 163208 | 全球油气能源LOF | "全球油气"/"油气"→0.399439 | 全球油气 QDII，非境内能源指数 |
| 164212 | 全球新能源车LOF | "新能源车"→0.399417（国证新能源车） | 全球 QDII，非国证指数 |
| 501225 | 全球芯片LOF | "芯片"→0.980017 | 全球芯片 QDII，非中华交易服务芯片 |

**拟处理**：4 只加入 `_ACTIVE_FUND_BLACKLIST`（与其余 11 只同类处理，改走 holdings/formal_lag）；501312 海外科技**保留** fx+idx（纳指代理近似合理），报告标「⚠️待核实」待人工最终确认。

### 修复 3：`实时估值计算报告.md` 同步修正（文档一致性）

- index 表 **18 只**「无行情」实为「可行情」→ 显示真实 `2.xxx` secid、状态改正常。
- fx+idx 表 **2 只**（160416、161128）→ 改为「代理 100.SPX」。
- 160925、501050 → 改为「可行情 `2.CES300`/`2.950090`」。
- 仅 **7 只**保留「无行情」（真实无行情源）。
- 行 28 状态分布修正为与表格一致；160644/163208/164212/501225 从「待核实」改为「主动型黑名单」。

---

## 三、不做的改动（保持现状）

1. **不删除** 30 条 `2.xxx` secid——实测有效，是修活中证主题指数的关键。
2. **不动** `_HK_INDEX_SECIDS` / `_OVERSEAS_INDEX_SECIDS` 的判定逻辑。
3. **不重构** `match_index` / `estimate_funds` 主流程，仅做数据与黑名单修正。
4. **不新增** 7 只真实无行情基金的强行估算（无实时报价源，退化 holdings/formal_lag 为合理兜底）。
5. **不改** 160416/161128 的 `100.SPX` 代理（海外行业指数无精确行情，SPX 是现有合理近似）。

---

## 四、实施后的验证方式

1. 本地重启估算服务，确认 `160925`、`501050` 返回 `est_source="index"` 且 `index_secid` 正确。
2. 确认 4 只新入黑名单基金（160644/163208/164212/501225）不再走 index，改走 holdings/formal_lag。
3. 确认其余 169 条映射估算结果与改动前一致（无回归）。
4. 重新生成报告文档，核对各类数量与状态标注（跟踪表 32 只「无行情」→ 25 只改标 + 7 只保留）。

---

## 五、主 agent 确认结果（2026-08-07）

- [x] 同意修复 1：补充 160925、501050 两条映射。✅ 已实施（`2.CES300` / `2.950090`），本地 `estimate_funds` 验证返回 `est_source=index`。
- [x] 同意修复 2：将 160644、163208、164212、501225 加入主动型黑名单；501312 保留 fx+idx 待人工确认。✅ 已实施（另含 501226 防御保留），本地验证 5 只均返回 `formal_lag`，不再误走 index。
- [x] 同意修复 3：同步修正报告文档。✅ 已同步：index/fx+idx 表 20 只修活 + 5 只代理 + 15 只黑名单 + 7 只保留无行情 + 1 只待核实；说明行统计数字已与表格对齐。
- [ ] 收尾流程：本地验证 ✅ → 重建并部署远程 NAS（待确认执行）→ 清理镜像 → 提交 git。
